<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title> Search </title>
    </head>
    <body>
    
        <form method="GET" action="/search">
           <div class="block" style="margin-left: 40%; margin-right: 40%; margin-top: 10%">
                <div class="field">
                  <label class="label">Слово на английском</label>
                  <div class="control">
                    <input class="input" type="text" name="key" value="#(key)" placeholder="Введите слово">
                  </div>
                </div>
                
                <div class="field">
                  <label class="label">Требуемый язык перевода</label>
                  <div class="control">
                    <input class="input" type="text" name="language" value="#(language)" placeholder="Введите язык">
                  </div>
                </div>
                
                <div class="control">
                  <button type="submit" class="button is-primary">Найти</button>
                </div>
            </div>
        </form>
    </body>
</html>